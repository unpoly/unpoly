- Update docs
- Get rid of u.addClass, u.writeInlineStyle, u.fastHide, etc. if possible
- Move all remaining element-related helper methods from util to query
- Remove event.element, event.$element, event.$target options for up.emit()
- Check if we really need to do q.element(...) in every single function that works with elements
- Move duplicate u.element when we already have q.element


