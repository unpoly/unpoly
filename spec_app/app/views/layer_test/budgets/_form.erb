<%= form_for [:layer_test, @budget], html: { 'up-follow': '' } do |form| %>

  <%= form.label :name %>
  <%= form.text_field :name %>

  <hr>

  <%= form.label :amount %>
  <%= form.text_field :amount %>

<%= form.submit "Save" %>

<% end %>
