<h2>All projects</h2>

<%= link_to 'New project', new_nested_form_test_project_path %>

<ul>
  <% @projects.each do |project| %>
    <li>
      <%= link_to project.name, [:nested_form_test, project] %>
    </li>
  <% end %>
</ul>