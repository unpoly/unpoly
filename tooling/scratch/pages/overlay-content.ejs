<%# Also reachable directly at /overlay-content, which is worth knowing: the same response
    serves as an overlay's content and as a full page. %>
<h1>Overlay content</h1>

<p>Opened in an overlay. <a href="#" up-dismiss>Dismiss</a></p>
