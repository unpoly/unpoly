<%# Two pages that replace each other. Neither carries an [up-target]: the layout's <main>
    matches up.fragment.config.mainTargets, so a plain [up-follow] updates exactly that. %>
<h1>Navigation: page one</h1>

<nav up-nav>
  <a href="/navigation-one" up-follow>Page one</a>
  <a href="/navigation-two" up-follow>Page two</a>
</nav>

<p>The link to the page you are on is marked <code>.up-current</code>.</p>
