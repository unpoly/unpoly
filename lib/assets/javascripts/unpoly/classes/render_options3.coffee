#class up.RenderOptions3
#
#  constructor: ->
#
#  merge: ->
#
#  deriveFailOptions: ->
#
#  @create: (defaults, userOptions) ->
#    base = new this()
#    withDefaults = Object.create(base, defaults)
#    withUserOptions = Object.create(withDefaults, userOptions)
