###**
@module up
###
window.up =
  version: <%= Unpoly::Rails::VERSION.to_json %>

  deprecateRenamedModule: (oldName, newName) ->
    Object.defineProperty? up, oldName, get: ->
      up.log.warn("Deprecated: up.#{oldName} has been renamed to up.#{newName}")
      up[newName]
