// function formatElement(object) {
//   try {
//     if (up.util.isElement(object)) {
//       return up.fragment.toTarget(object)
//     }
//   } catch (error) {
//     // Don't blow up the test runner when up.util.isElement() or up.fragment.toTarget() has a bug.
//     return undefined
//   }
// }
//
// beforeEach(function() {
//   jasmine.addCustomObjectFormatter(formatElement)
// })
