describe('up.Record', function() {
  describe('defaults', function() {

    it('is used for missing constructor props')

    it('can be overridden by constructor props')

    it('always copies in case #defaults() always returns the same object')
  })
})

